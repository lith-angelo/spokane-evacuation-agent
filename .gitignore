__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.env
*.sqlite3

# Session state is regenerated on every run; the replay fixtures under
# data/fixtures/ are the demo and must stay tracked.
data/sessions.sqlite3
data/*.sqlite3

web/node_modules/
web/dist/

# Workstation metadata is never part of the submission.
.DS_Store
._*

# Local experiments used during development, but not imported by the runtime
# or included in the hackathon submission.
data/training/
scripts/harvest_fire_growth.py
